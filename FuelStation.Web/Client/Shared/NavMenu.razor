@inject UserStatus userStatus
@using Model.Enums;
<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">BlazorWasmAuthenticationAndAuthorization</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass" @onclick="ToggleNavMenu">
    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="oi oi-home" aria-hidden="true"></span> Home
            </NavLink>
        </div>
        <AuthorizeView Roles="Administrator,User">
            <Authorized>
                <div class="nav-item px-3">
                    <NavLink class="nav-link" href="counter">
                        <span class="oi oi-plus" aria-hidden="true"></span> Counter
                    </NavLink>
                </div>
            </Authorized>
        </AuthorizeView>
        <AuthorizeView Roles="Administrator">
            <Authorized>
                <div class="nav-item px-3">
                    <NavLink class="nav-link" href="fetchdata">
                        <span class="oi oi-list-rich" aria-hidden="true"></span> Fetch data
                    </NavLink>
                </div>
            </Authorized>
        </AuthorizeView>
    </nav>
</div>


@*<div class="@NavMenuCssClass nav-scrollable" @onclick="ToggleNavMenu">
    <div class="nav-item px-3">


       <ul class="nav flex-column">
            @if (userStatus.Role == null) {
                <li class="nav-item px-3">
                    <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                        <span class="oi oi-account-login" aria-hidden="true"></span> Login
                    </NavLink>
                </li>
            } else {
                <li class="nav-item px-3">
                    <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                        <span class="oi oi-account-login" aria-hidden="true"></span> Change User
                    </NavLink>
                </li>
            }

            <AuthorizeView Roles="Administrator,User">
                <Authorized>
            @if (userStatus.Role == EmployeeType.Manager) {
                <li class="nav-item px-3">
                    <NavLink class="nav-link" href="employeelist">
                        <span class="oi oi-people" aria-hidden="true"></span> Employees
                    </NavLink>
                </li>
                <li class="nav-item px-3">
                    <NavLink class="nav-link" href="ledgerlist">
                        <span class="oi oi-calendar" aria-hidden="true"></span> Monthly Ledger
                    </NavLink>
                </li>
            }
                 </Authorized>
            </AuthorizeView>
            
            @if(userStatus.Role == EmployeeType.Cashier || userStatus.Role == EmployeeType.Manager){
                <li class="nav-item px-3">
                        <NavLink class="nav-link" href="customerlist">
                            <span class="oi oi-people" aria-hidden="true"></span> Customers
                        </NavLink>
                </li>
            }
            
            @if(userStatus.Role == EmployeeType.Staff || userStatus.Role == EmployeeType.Manager){
                <li class="nav-item px-3">
                     <NavLink class="nav-link" href="itemlist">
                        <span class="oi oi-list" aria-hidden="true"></span> Items
                     </NavLink>
                </li>
            }
        </ul>
    </div>
</div>*@

    

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu() {
        collapseNavMenu = !collapseNavMenu;
    }

}
